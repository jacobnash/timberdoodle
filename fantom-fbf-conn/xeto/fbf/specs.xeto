// FBF bridge connector rec
FbfConn: Conn {
  fbfConn
}

// Point synchronized via the FBF MQTT bridge - fbfCur holds the exact
// MQTT topic FBF's bridge publishes readings on (e.g.
// "fbf/mock-ahu-1/analogValue,1").
FbfPoint: ConnPoint {
  fbfConnRef: Ref? <of:FbfConn>
  fbfCur: Str?
}
