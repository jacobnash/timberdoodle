x-daemon-env: &daemon-env
  TIMBERDOODLE_PG_DSN: postgresql://timberdoodle:${POSTGRES_PASSWORD:-timberdoodle}@postgres:5432/timberdoodle
  OXIGRAPH_URL: http://oxigraph:7878
  MQTT_HOST: mosquitto
  OTEL_EXPORTER_OTLP_ENDPOINT: http://jaeger:4318/v1/traces
  TIMBERDOODLE_ALLOW_PRIVATE_WEBHOOKS: ${TIMBERDOODLE_ALLOW_PRIVATE_WEBHOOKS:-}
  # Blank-safe like the var above, not required (:?) - only auth_api.py
  # and the gateway need this so far (incremental gateway-auth rollout,
  # see the plan doc's Phase 2 migration path); other daemons never
  # check it. Set it in .env once you're actually using auth_api/the new
  # gateway auth - until then the stack runs exactly as before.
  TIMBERDOODLE_GATEWAY_SECRET: ${TIMBERDOODLE_GATEWAY_SECRET:-}
  # Only auth_api.py (issues tokens) actually needs this - blanket-added
  # to the anchor like the var above, harmless for daemons that never
  # read it.
  TIMBERDOODLE_JWT_SECRET: ${TIMBERDOODLE_JWT_SECRET:-}

x-daemon-depends-on: &daemon-depends-on
  postgres:
    condition: service_healthy
  oxigraph:
    condition: service_started
  mosquitto:
    condition: service_started

services:
  mosquitto:
    image: eclipse-mosquitto:2
    command: mosquitto -c /mosquitto-no-auth.conf
    ports:
      - "1883:1883"

  oxigraph:
    image: ghcr.io/oxigraph/oxigraph:latest
    command: serve -l /data -b 0.0.0.0:7878 --cors --union-default-graph
    volumes:
      - oxigraph-data:/data
    ports:
      - "7878:7878"

  postgres:
    image: timescale/timescaledb:latest-pg16
    command: postgres -c log_statement=all
    environment:
      POSTGRES_USER: timberdoodle
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-timberdoodle}
      POSTGRES_DB: timberdoodle
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U timberdoodle"]
      interval: 2s
      timeout: 3s
      retries: 20

  jaeger:
    image: jaegertracing/all-in-one:latest
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    ports:
      - "16686:16686" # UI
      - "4318:4318" # OTLP HTTP

  grafana:
    image: grafana/grafana-oss:latest
    environment:
      GF_AUTH_ANONYMOUS_ENABLED: ${GRAFANA_ANON_ENABLED:-false}
      GF_AUTH_ANONYMOUS_ORG_ROLE: "Admin"
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-timberdoodle}
      # Grafana's newer apiserver-backed provisioning path (feature toggle
      # "provisioning", on by default in recent grafana-oss:latest) throws
      # "Datasource provisioning error: data source not found" against our
      # classic file-based grafana/provisioning/ YAML - disabling it falls
      # back to the classic provisioner, which works correctly.
      GF_FEATURE_TOGGLES_DISABLE: "provisioning"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "3033:3000"

  mqtt_listener:
    build: .
    command: python -m timberdoodle.mqtt_listener
    environment: *daemon-env
    volumes:
      - .:/app
    depends_on: *daemon-depends-on

  ingest_api:
    build: .
    command: python -m timberdoodle.ingest_api
    environment: *daemon-env
    volumes:
      - .:/app
    depends_on: *daemon-depends-on

  fault_api:
    build: .
    command: python -m timberdoodle.fault_api
    environment: *daemon-env
    volumes:
      - .:/app
    depends_on: *daemon-depends-on

  fault_detector:
    build: .
    command: python -m timberdoodle.fault_detector
    environment: *daemon-env
    volumes:
      - .:/app
    depends_on: *daemon-depends-on

  derivation_api:
    build: .
    command: python -m timberdoodle.derivation_api
    environment: *daemon-env
    volumes:
      - .:/app
    depends_on: *daemon-depends-on

  derivation_engine:
    build: .
    command: python -m timberdoodle.derivation_engine
    environment: *daemon-env
    volumes:
      - .:/app
    depends_on: *daemon-depends-on

  validate_api:
    build: .
    command: python -m timberdoodle.validate_api
    environment: *daemon-env
    volumes:
      - .:/app
    depends_on: *daemon-depends-on

  auth_api:
    build: .
    command: python -m timberdoodle.auth_api
    environment: *daemon-env
    volumes:
      - .:/app
    depends_on: *daemon-depends-on

  haystack_puller:
    build: .
    command: python -m timberdoodle.haystack_puller
    profiles: ["haystack-pull"]
    environment:
      <<: *daemon-env
      REMOTE_HAYSTACK_URL: ${REMOTE_HAYSTACK_URL:-}
      REMOTE_HAYSTACK_USER: ${REMOTE_HAYSTACK_USER:-}
      REMOTE_HAYSTACK_PASSWORD: ${REMOTE_HAYSTACK_PASSWORD:-}
      REMOTE_HAYSTACK_SOURCE_ID: ${REMOTE_HAYSTACK_SOURCE_ID:-haxall}
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy
      oxigraph:
        condition: service_started

  gateway:
    image: nginx:alpine
    ports:
      - "8080:80"
    environment:
      # Required now (not blank-safe like the daemon-env anchor's copies
      # of these) - every location is cut over to js_access as of the
      # Phase 2 migration path's final step, so an unset secret here
      # means the gateway can't verify anything at all, not just one
      # not-yet-migrated route.
      TIMBERDOODLE_JWT_SECRET: ${TIMBERDOODLE_JWT_SECRET:?set in .env - see .env.example}
      TIMBERDOODLE_GATEWAY_SECRET: ${TIMBERDOODLE_GATEWAY_SECRET:?set in .env - see .env.example}
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./gateway/njs:/etc/nginx/njs:ro
      - ./ui:/usr/share/nginx/ui:ro
    depends_on:
      - ingest_api
      - fault_api
      - derivation_api
      - validate_api
      - auth_api

volumes:
  oxigraph-data:
  postgres-data:
  grafana-data:
